apiVersion: v2
name: springboot-app
description: A Helm chart for deploying a Spring Boot application using the common library.
version: 0.1.0
appVersion: "1.0.0"
dependencies:
  - name: common
    version: "0.1.0"
    repository: "file://../common"
